import { createPersonalWorkspace } from './createPersonalWorkspace';
import { checkSlugAvailability } from './checkSlugAvailability';
import { getMyWorkspaces } from './getMyWorkspaces';

export const workspaceService = {
    createPersonalWorkspace,
    checkSlugAvailability,
    getMyWorkspaces
}

export {
    createPersonalWorkspace,
    checkSlugAvailability,
    getMyWorkspaces
}